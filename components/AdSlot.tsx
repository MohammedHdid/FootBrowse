"use client"

/* eslint-disable @typescript-eslint/no-unused-vars */
// Ad slots disabled until units are approved in Google AdSense.
// To re-enable: restore the ins element and useEffect (see git history).
export default function AdSlot(_: {
  slot: string
  format?: 'auto' | 'rectangle' | 'horizontal'
  className?: string
}) {
  return null
}
